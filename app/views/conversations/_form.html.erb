<%= form_with(model: conversation) do |form| %>
  <% if conversation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(conversation.errors.count, "error") %> prohibited this conversation from being saved:</h2>

      <ul>
        <% conversation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :chat %>
    <%= form.text_field :chat %>
  </div>

  <div class="field">
    <%= form.label :fecha_texto %>
    <%= form.date_select :fecha_texto %>
  </div>

  <div class="field">
    <%= form.label :leads_id %>
    <%= form.text_field :leads_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
